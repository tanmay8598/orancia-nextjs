import React from "react";

const MarqueeChild = ({ title }) => {
  return (
    <>
      <div className="p-2 font-semibold text-xl text-red-400 ">{title}</div>
    </>
  );
};

export default MarqueeChild;
